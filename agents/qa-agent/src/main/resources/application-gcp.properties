spring.datasource.url=${QA_DB_URL:jdbc:postgresql://localhost:5432/qa_agent}
spring.datasource.username=${QA_DB_USERNAME:postgres}
spring.datasource.password=${QA_DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.sql.init.mode=never
